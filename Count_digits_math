def count_digits(num):
    count=0

    while num>0:
        lastdigit=num%10
        count+=1
        num//=10
    return count

num=int(input("Enter the number:"))
print("Number of digits:" ,count_digits(num))
